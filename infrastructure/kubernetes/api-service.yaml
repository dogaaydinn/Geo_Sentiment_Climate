apiVersion: v1
kind: Service
metadata:
  name: geo-climate-api-service
  namespace: geo-climate
  labels:
    app: geo-climate-api
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: geo-climate-api
